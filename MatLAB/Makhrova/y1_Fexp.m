tau1 = 16; %определение параметров 
tau2 = 5;
A = 1;
t = -10:1:100; %временной итервал 
y1 = Fexp(tau1, tau2, A, t); %задаем функцию

l = 6; %параметры фильтра
k = 13;
m1 = 15;
m2 = 1;

y2 = filter(l, k, m1, m2, y1);
plot(t, y1, 'b');%строим график
figure;
plot(t, y2, 'r');

